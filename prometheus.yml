global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'urlshortener-backend'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['urlshortener-backend:8080']
